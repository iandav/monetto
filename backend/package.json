{
  "name": "monetto-backend",
  "version": "1.0.0",
  "description": "Backend API for Monetto",
  "main": "index.js",
  "dependencies": {
    "@prisma/client": "^3.12.0",
    "dotenv": "^16.0.0",
    "express": "^4.17.3"
  },
  "scripts": {
    "api": "nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "prisma": "^3.12.0"
  }
}
